<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <group
        android:id="@+id/main_menu_group"
        android:orderInCategory="2">

        <item
            android:id="@+id/action_friend_requests_item"
            app:showAsAction="ifRoom"
            android:title="Friend Requests"
            app:actionLayout="@layout/action_badge_button"
            android:icon="@drawable/ic_group_24dp"/>

        <item
            android:id="@+id/action_bar_profile_item"
            app:showAsAction="never"
            android:title="@string/action_bar_profile"/>

        <item
            android:id="@+id/action_bar_about_item"
            app:showAsAction="never"
            android:title="@string/action_bar_about"/>

        <item
            android:id="@+id/action_bar_logout_item"
            app:showAsAction="never"
            android:title="Logout"/>

    </group>

</menu>